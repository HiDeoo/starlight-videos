---
import { Icon } from '@astrojs/starlight/components'

import type { CollectionVideoEntry } from '../libs/video'

interface Props {
  collectionVideos: CollectionVideoEntry[]
}

const { collectionVideos } = Astro.props

// TODO(HiDeoo) i18n + pluralize
---

<div><Icon name="list-format" size="1rem" />{collectionVideos.length} videos</div>

<style>
  div {
    align-items: center;
    display: flex;
    gap: 0.3125rem;
  }
</style>
