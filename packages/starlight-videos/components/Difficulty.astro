---
import type { CollectionVideo, Video } from '../schemas'

interface Props {
  value: (Video | CollectionVideo)['difficulty']
}

const { value } = Astro.props

// TODO(HiDeoo) sr-only i18n description
---

<div>{value}</div>

<style>
  div {
    align-items: center;
    display: flex;
  }
</style>
