---
import type { Props } from '@astrojs/starlight/props'

import { isEntryWithVideo } from '../libs/video'

const { entry } = Astro.props

// TODO(HiDeoo) notices position
---

{isEntryWithVideo(entry) && <div>video</div>}
