---
import type { CollectionVideo, Video } from '../schemas'

type Duration = (Video | CollectionVideo)['duration']

interface Props {
  value: Duration | Duration[]
}

const { value } = Astro.props
---

<div>Duration: {Array.isArray(value) ? value.join(', ') : value}</div>
